---
interface Props {
    href: string
    title: string
}

const { href, title } = Astro.props
const target = href.startsWith('http') ? '_blank' : null
const rel = target ? 'noopener noreferrer' : null
---
<a class="site-link" href={href} title={title} target={target} rel={rel}>
    <slot/>
</a>

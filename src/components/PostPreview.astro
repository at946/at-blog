---
import type { CollectionEntry } from 'astro:content';
import { Icon } from 'astro-icon/components';
import { getIcon } from '@/utils/getIcon';
import normalizeMarkdoc from '@/utils/normalizeMarkdoc';
import estimateReadingTime from '../utils/estimateReadingTime';
import Link from './mdoc/Link.astro';

interface Props {
	post: CollectionEntry<'blog'>;
	class?: string;
}

const { post } = Astro.props;
const { title, publicationDate, externalUrl, type, tags } = post.data;
const id: string = post.id;
const plainText: string = normalizeMarkdoc(post.body || '');
const readingTime: number = estimateReadingTime(plainText.length);
---

<Link
  href={externalUrl ?? `/blog/${id}`}
  class:list={[
    "text-left border-gray-500 rounded p-3 transition duration-300",
    "hover:text-primary hover:border-primary",
    "focus-visible:text-primary focus-visible:border-primary",
    "dark:border-white",
    "dark:hover:border-primary",
    Astro.props.class
  ]}
>
  <div class="mb-2 font-medium text-sm flex justify-between">
    <span>
      { new Intl.DateTimeFormat('ja-JP', { dateStyle: 'long' }).format(publicationDate)}
    </span>
  { !externalUrl &&
    <span class="text-sm flex items-center gap-1">
      <Icon name="mdi:clock-outline" class="size-4" />
      <span>{ readingTime }min</span>
    </span>
  }
  </div>
  <h2 class="text-xl font-bold mb-4">
      <Icon name={getIcon(type)} class="inline align-middle mr-2" />
      <span class="align-middle">
        {title}
      </span>
      { externalUrl && <Icon name="mdi:external-link" class="inline align-middle ml-2" /> }
  </h2>
      {
        !!tags && (
          <div class="flex flex-wrap gap-2 justify-center md:justify-start">
            {tags.map((tag) => (<span class="text-sm font-medium">#{tag}</span>))}
          </div>
        )
      }
</Link>
